
name: Test Docker-in-Docker Runtime

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ (github.head_ref && github.ref) || github.run_id }}
  cancel-in-progress: true

jobs:
  test-dind-runtime:
    runs-on: ubuntu-latest

    services:
      docker:
        enabled: true
        options: --privileged -v /var/run/docker.sock:/var/run/docker.sock

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        pip install poetry
        poetry install --only main,dev

    - name: Test Docker-in-Docker runtime build
      env:
        INSTALL_DOCKER: 0
        RUNTIME: local
      run: |
        python -m openhands.runtime.utils.runtime_build \
          --base_image ubuntu:24.04 \
          --force_rebuild

    - name: Test Docker-in-Docker functionality in container
      env:
        INSTALL_DOCKER: 0
        RUNTIME: local
      run: |
        python -m openhands.runtime.impl.docker.docker_runtime \
          --base_image ubuntu:24.04 \
          --test_dind

    - name: Verify Docker is accessible inside the container
      env:
        INSTALL_DOCKER: 0
        RUNTIME: local
      run: |
        python -c "import docker; client = docker.from_env(); print('Docker client created successfully'); print(f'Docker version: {client.version()}')"
