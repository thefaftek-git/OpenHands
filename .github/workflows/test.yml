# This workflow is disabled for now as we're only running the dind test
name: Run Tests

on:
  push:
    branches-ignore:
      - '*'  # Ignore all branches
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          echo "Skipping test.yml workflow"
          exit 0
